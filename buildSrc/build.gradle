apply plugin: 'groovy'
apply plugin: 'idea'

repositories {
    mavenCentral()
}

dependencies {
    compile gradleApi()
    compile localGroovy()
    compile(group: 'org.hidetake', name: 'groovy-ssh', version: '2.9.0') {
        exclude group: 'org.codehaus.groovy', module: 'groovy-all'
    }
}
